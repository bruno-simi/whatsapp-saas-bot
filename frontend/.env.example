# Leave empty: browser uses /api -> Next.js proxies to http://127.0.0.1:4000 (works with LAN IP too).
# NEXT_PUBLIC_API_URL=

# Or point the browser straight at the API (you must allow CORS for your frontend origin on the backend):
# NEXT_PUBLIC_API_URL=http://localhost:4000
